{
    "schemaVersion": 1,
    "id": "${mod_id}-test",
    "version": "${version}",
    "name": "${mod_name} GameTests",
    "environment": "*",
    "entrypoints": {
        "fabric-gametest": [
            "dev.riftal.creator.gametest.FabricGameTests"
        ]
    },
    "depends": {
        "${mod_id}": "*",
        "minecraft": "${minecraft_version}"
    }
}
