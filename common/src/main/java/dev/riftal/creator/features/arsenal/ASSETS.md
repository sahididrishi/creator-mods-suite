# creator_arsenal — asset inventory

Nothing in this list is finished art. Every PNG is generated by `tools/make_placeholders.py`
(python3 stdlib only) and every sound by `tools/make_sounds.sh` (ffmpeg synthesis only). They are
deliberately flat — three tones per material, a hard one-pixel key line, no shading ramps, no
sampled audio — but they are **readable**: each sprite carries the silhouette of the weapon it
stands for and each sound is the right shape for its role, so a placeholder take still reads on
camera. One palette runs through all nine textures (seven item sprites plus the hook and the arrow); it is documented at the top of the generator.

| File | Kind | Status | What a real artist should do |
|---|---|---|---|
| `assets/creator_arsenal/textures/item/grapple_blade.png` | 16x16 PNG | **PROCEDURAL PLACEHOLDER** (`tools/make_placeholders.py`) | Steel short-sword on the tool diagonal, rope wound round the grip, a small barbed hook hanging off the pommel |
| `assets/creator_arsenal/textures/item/storm_bow.png` | 16x16 PNG | **PROCEDURAL PLACEHOLDER** | Dark wood recurve bow, cyan string, arcing sparks at the nocks |
| `assets/creator_arsenal/textures/item/storm_bow_pulling_0.png` | 16x16 PNG | **PROCEDURAL PLACEHOLDER** | Same bow at first draw: limbs just starting to bend, string barely back |
| `assets/creator_arsenal/textures/item/storm_bow_pulling_1.png` | 16x16 PNG | **PROCEDURAL PLACEHOLDER** | Half draw: clear limb bend, string pulled to a V |
| `assets/creator_arsenal/textures/item/storm_bow_pulling_2.png` | 16x16 PNG | **PROCEDURAL PLACEHOLDER** | Full draw - the state that calls lightning. It must be obviously different from the half draw; the 12-20 s beat is that contrast |
| `assets/creator_arsenal/textures/item/gravity_hammer.png` | 16x16 PNG | **PROCEDURAL PLACEHOLDER** | Obsidian maul head on a dark haft, purple light bleeding from the seams |
| `assets/creator_arsenal/textures/item/soul_scythe.png` | 16x16 PNG | **PROCEDURAL PLACEHOLDER** | Bone snath, teal soul-fire edge, wisps curling off the blade |
| `assets/creator_arsenal/textures/entity/grapple_hook.png` | 16x16 PNG | **PROCEDURAL PLACEHOLDER** | Three-pronged iron hook, drawn to read at ~8 px on screen (it renders as one camera-facing quad) |
| `assets/creator_arsenal/textures/entity/projectiles/storm_arrow.png` | 32x32 PNG | **PROCEDURAL PLACEHOLDER** | Vanilla arrow layout, cyan fletching, a charged glow along the shaft. **Only two regions are ever sampled** (read out of `ArrowRenderer`): the shaft strip at pixels (0,0)–(15,4) and the fletching cross at (0,5)–(4,9). Anything drawn elsewhere is invisible. |
| `assets/creator_arsenal/sounds/arsenal/hook_bite.ogg` | 0.30 s Vorbis, **mono** | **PROCEDURAL PLACEHOLDER** (`tools/make_sounds.sh`, filtered noise + damped sines) | A hook biting stone: a sharp metal tick with a short stone-scrape tail. **Mono, 44.1 kHz.** |
| `assets/creator_arsenal/sounds/arsenal/slam_impact.ogg` | 1.30 s Vorbis, **mono** | **PROCEDURAL PLACEHOLDER** (descending sine + brown noise) | The hammer touchdown: a deep body-hit thud with a debris tail, no metallic ring (the vanilla anvil clang is layered over it in code). **Mono, 44.1 kHz.** |
| `assets/creator_arsenal/sounds/arsenal/soul_absorb.ogg` | 0.45 s Vorbis, **mono** | **PROCEDURAL PLACEHOLDER** (rising sine pair) | A soul being swallowed: a short breathy rise with a glassy top. **Mono, 44.1 kHz.** |
| `assets/creator_arsenal/sounds.json` | JSON | HAND-WRITTEN, FINAL | — |
| `assets/creator_arsenal/models/item/grapple_blade.json` | JSON | HAND-WRITTEN, FINAL | — |
| `assets/creator_arsenal/models/item/gravity_hammer.json` | JSON | HAND-WRITTEN, FINAL | — |
| `assets/creator_arsenal/models/item/soul_scythe.json` | JSON | HAND-WRITTEN, FINAL | — |
| `assets/creator_arsenal/models/item/storm_bow.json` | JSON | HAND-WRITTEN, FINAL | Display transforms and the `pulling`/`pull` overrides (1 / 0.65 / 0.9) match vanilla's bow so it sits right in both hands and draws on the same thresholds |
| `assets/creator_arsenal/models/item/storm_bow_pulling_0.json` | JSON | HAND-WRITTEN, FINAL | - |
| `assets/creator_arsenal/models/item/storm_bow_pulling_1.json` | JSON | HAND-WRITTEN, FINAL | - |
| `assets/creator_arsenal/models/item/storm_bow_pulling_2.json` | JSON | HAND-WRITTEN, FINAL | - |
| `assets/creator_arsenal/lang/en_us.json` | JSON | HAND-WRITTEN, FINAL | — |
| `data/creator_arsenal/structure/empty.nbt` | NBT | SCAFFOLD (shipped with the repo) | — |

Regenerate the placeholders:

```bash
python3 common/src/main/java/dev/riftal/creator/features/arsenal/tools/make_placeholders.py
bash    common/src/main/java/dev/riftal/creator/features/arsenal/tools/make_sounds.sh
```

**Audio: mono, positional (fixed).** All of these `.ogg` files are **1-channel, 44.1 kHz
Ogg Vorbis**, so Minecraft applies normal 3D positional attenuation and panning to them. They were
converted with `ffmpeg -ac 1 -ar 44100` piped into `oggenc` (vorbis-tools) — ffmpeg's *native*
`vorbis` encoder is still stereo-only and cannot be used on its own; see CONTRACT.md §9.2.
Replacement assets must stay **mono, 44.1 kHz**.

## Sounds this feature plays

Three are ours (above); the rest are vanilla and need no asset:
`entity.fishing_bobber.throw` (hook fired), `item.trident.return` (line cut),
`entity.lightning_bolt.impact` and the bolt's own thunder, `block.beacon.power_select` (hammer
lift), `block.anvil.land` (slam), `entity.player.attack.sweep` (scythe sweep),
`particle.soul_escape` (a soul leaving a corpse).

## How the bow draw animation is wired

Vanilla's `pull` / `pulling` model predicates are registered through
`ItemProperties.register(Item, ResourceLocation, ClampedItemPropertyFunction)`, which is
**`private`** in the 1.21.1 sources `:common` compiles against
(`net/minecraft/client/renderer/item/ItemProperties.java:53`), and the two predicates are registered
for `Items.BOW` specifically rather than generically (same file, lines 97 and 113), so a third-party
bow cannot borrow them from JSON either.

The escape hatch is this feature's own mixin config: `ArsenalItemPropertiesMixin` is a static
`@Invoker` on that method — the same trick `RulesGameRulesMixin` uses for `GameRules#register`, and
the only spelling that is identical on Fabric and NeoForge without editing a shared access widener
or access transformer. `StormBowProperties.register()` is then called from
`ArsenalFeature.initClient()`, which both loaders reach after the item registry has been flushed, so
`STORM_BOW.get()` is bound. No core change was needed.

## Known gaps

* **No GameTest arenas of our own.** Only the shared `data/creator_arsenal/structure/empty.nbt`
  exists, and every GameTest works inside it — the grapple test builds its own wall with
  `helper.setBlock` rather than shipping a `cliff_gap` template. Plan 07's `arena_16x8` and
  `cliff_gap` templates need a dev client (`/test create` + `/test export`) and are not required by
  anything currently in the suite.
* **No GeckoLib models.** Both entities render with vanilla-style renderers (a camera-facing quad
  plus a `RenderType.lineStrip()` rope for the hook, `ArrowRenderer` for the arrow), so there is no
  `geo/` or `animations/` directory and none is needed. Plan 07 lists 3D scythe/hammer models as a
  stretch goal.
* **No Better Combat `weapon_attributes` JSON.** Stretch goal in plan 07 section 3; skipped under
  the MVP-only rule.
* **No `creator_arsenal:grapple_hook` *item*.** Plan 07's section 5 registry table lists one as an
  icon for the hook renderer, but `GrappleHookRenderer` draws a camera-facing quad textured with
  `textures/entity/grapple_hook.png` instead of an `ItemRenderer.renderStatic` of a held stack, so
  registering the item would put an unobtainable, untranslatable entry in the item registry that
  nothing reads. The texture exists and is listed above; only the registry entry is deliberately
  absent. Plan 07's table and this are the two halves that disagree.
