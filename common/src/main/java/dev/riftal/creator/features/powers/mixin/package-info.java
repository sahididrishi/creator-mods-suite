/**
 * Mixins owned by the {@code powers} feature. Declared in
 * {@code common/src/main/resources/creatormods-powers.mixins.json}, which is already registered in
 * {@code fabric.mod.json} and {@code neoforge.mods.toml} - append class names to that file's
 * {@code "mixins"} or {@code "client"} array, never to another feature's.
 */
package dev.riftal.creator.features.powers.mixin;
