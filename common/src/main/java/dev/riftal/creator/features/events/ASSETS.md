# creator_events — asset inventory

Everything this feature references at runtime now exists on disk. The JSON is hand-written and
final; the one texture and the six sounds are **procedural placeholders**, generated by the two
scripts in `tools/` and deliberately obvious about it.

Regenerate them with:

```bash
python3 common/src/main/java/dev/riftal/creator/features/events/tools/make_placeholder_textures.py
bash    common/src/main/java/dev/riftal/creator/features/events/tools/make_placeholder_sounds.sh
```

`EventAssetCoverageTest` (in `common/src/test/.../features/events/`) fails the build if any of the
files below goes missing, if a sound event loses its `sounds.json` entry or its subtitle, or if a
translation key this feature emits has no line in the lang file.

## Textures

| File | Kind | Status | What a real artist should do |
|---|---|---|---|
| `assets/creator_events/textures/block/lucky_rain.png` | 16×16 RGBA PNG | **PROCEDURAL PLACEHOLDER** (`tools/make_placeholder_textures.py`) | Cast-gold block face, hard dark bevel, an embossed `?` catching a highlight from the top-left; it should read as "open me" at 3 blocks and at 30 |

There is **no meteor entity texture and none is needed**: `MeteorRenderer` draws a scaled, tumbling
`minecraft:magma_block` through `BlockRenderDispatcher#renderSingleBlock` and returns
`TextureAtlas.LOCATION_BLOCKS` from `getTextureLocation`. A bespoke Blockbench model and a 32×32
texture are the stretch goal; nothing in the code has to change to adopt them beyond the renderer.

No GeckoLib model, animation or particle JSON: the meteor is the only custom entity, it is not a
GeckoLib entity, and every particle used is vanilla (`FLAME`, `LAVA`, `LARGE_SMOKE`,
`EXPLOSION_EMITTER`, `SQUID_INK`, `ASH`, `TOTEM_OF_UNDYING`).

## Sounds

All six are **mono, 44.1 kHz Ogg Vorbis**, so Minecraft applies normal distance attenuation and
panning — the drone and the hum fade with distance as intended. ffmpeg's native `vorbis` encoder is
stereo-only, so they are encoded with `ffmpeg -ac 1 -ar 44100` piped into `oggenc` (vorbis-tools),
per CONTRACT.md §9.2. Every replacement must stay **mono, 44.1 kHz**.

| File | Sound event | Kind | Status | What a real artist should do |
|---|---|---|---|---|
| `assets/creator_events/sounds/bloodmoon/drone.ogg` | `creator_events:bloodmoon.drone` | 6.0 s Vorbis, **mono** | **PROCEDURAL PLACEHOLDER** (55 Hz + fifth + slow beat) | Seamless loop: bowed sub-bass, distant wolves, a faint metallic shimmer. Re-triggered every 5 s, so it must not click at the seam |
| `assets/creator_events/sounds/meteor/whistle.ogg` | `creator_events:meteor.whistle` | 3.0 s Vorbis, **mono** | **PROCEDURAL PLACEHOLDER** (falling sweep + air noise) | Air-tearing descending howl, loud, mono, with the doppler already baked in |
| `assets/creator_events/sounds/meteor/impact.ogg` | `creator_events:meteor.impact` | 2.2 s Vorbis, **mono** | **PROCEDURAL PLACEHOLDER** (42 Hz thud + noise burst) | Deep ground-shaking boom, rock debris tail, a long low rumble underneath |
| `assets/creator_events/sounds/siege/horn.ogg` | `creator_events:siege.horn` | 2.4 s Vorbis, **mono** | **PROCEDURAL PLACEHOLDER** (two notes, D2 → G2) | A real war horn, two notes, slightly detuned, with a stone-courtyard tail |
| `assets/creator_events/sounds/lucky/pop.ogg` | `creator_events:lucky.pop` | 0.45 s Vorbis, **mono** | **PROCEDURAL PLACEHOLDER** (rising blip) | Short bright pop-and-sparkle; it fires on every landing block, so it must not fatigue |
| `assets/creator_events/sounds/void/hum.ogg` | `creator_events:void.hum` | 4.0 s Vorbis, **mono** | **PROCEDURAL PLACEHOLDER** (38 Hz + tremolo) | Sub-bass dread with a slow tremolo and a touch of noise; felt more than heard |

Sound event ids use the `<category>.<name>` convention, so the `sounds.json` keys are
`bloodmoon.drone`, `meteor.whistle`, `meteor.impact`, `siege.horn`, `lucky.pop`, `void.hum`, and the
files live one directory deeper (`sounds/bloodmoon/drone.ogg`).

## Hand-written, final

| File | Kind | Status |
|---|---|---|
| `assets/creator_events/lang/en_us.json` | JSON | HAND-WRITTEN, FINAL — one key per translatable string this feature emits |
| `assets/creator_events/sounds.json` | JSON | HAND-WRITTEN, FINAL — six events, each with a subtitle key |
| `assets/creator_events/blockstates/lucky_rain.json` | JSON | HAND-WRITTEN, FINAL |
| `assets/creator_events/models/block/lucky_rain.json` | JSON | HAND-WRITTEN, FINAL — `cube_all` over `creator_events:block/lucky_rain` |
| `assets/creator_events/models/item/lucky_rain.json` | JSON | HAND-WRITTEN, FINAL |
| `data/creator_events/loot_table/meteor.json` | JSON | HAND-WRITTEN, FINAL — the crater chest; nests `minecraft:chests/simple_dungeon` |
| `data/creator_events/loot_table/siege_reward.json` | JSON | HAND-WRITTEN, FINAL — dropped at the director's feet on victory |
| `data/creator_events/loot_table/blocks/lucky_rain.json` | JSON | HAND-WRITTEN, FINAL — the block drops itself when mined |
| `data/creator_events/lucky_outcomes/default.json` | JSON | HAND-WRITTEN, FINAL — falls back to `LuckyOutcomes.builtIn()` if removed |
| `data/creator_events/siege_waves/default.json` | JSON | HAND-WRITTEN, FINAL — falls back to `SiegeWaves.defaults()` if removed |
| `data/creator_events/structure/empty.nbt` | NBT | SCAFFOLD, shipped with the repo — the 9×9×9 GameTest arena. Do not overwrite |

## Tools

| File | What it is |
|---|---|
| `tools/make_placeholder_textures.py` | python3 stdlib only (`struct` + `zlib`). Writes every PNG above. |
| `tools/make_placeholder_sounds.sh` | ffmpeg `aevalsrc` synthesis. Writes every ogg above and prints a `ffprobe` verification table. |
